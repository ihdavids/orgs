todo_views:
  alltasks: "!IsProject() && IsTodo()"
  today:    "!IsProject() && IsTodo() && Today()"
  untagged: "!IsProject() && IsTodo() && NoTags()"
  next:     "!IsProject() && IsTodo() && IsStatus('NEXT')"
  projects: "IsProject()"
